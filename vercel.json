{
  "version": 2,
  "buildCommand": "npm install --omit=optional --no-audit --no-fund --force && npm run build",
  "installCommand": "npm install --omit=optional --no-audit --no-fund --force",
  "framework": "nextjs",
  "outputDirectory": ".next"
} 